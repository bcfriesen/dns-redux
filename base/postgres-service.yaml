# base/postgres-service.yaml
apiVersion: v1
kind: Service
metadata:
  name: pdns-postgres
  namespace: dns-system
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
  selector:
    app: pdns-postgres
